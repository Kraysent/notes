# How to test code

Run the `make test` target to execute all tests and checks for both backend and frontend code:

```bash
make test
```

# Automatically fix linter and formatter errors

Run the `make fix` target to automatically fix linter and formatter errors for both backend and frontend:

```bash
make fix
```
